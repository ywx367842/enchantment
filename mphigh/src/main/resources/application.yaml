spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mphigh?useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: root

logging:
  level:
    root: warn
    com.cesar.mphigh.user.dao: trace
  pattern:
    console: '%p%m%n'

  mapper-locations: classpath*:mybatis/*.xml
#    - mapper/*
#  global-config:
#    db-config:
#      id-type: uuid
#      field-strategy: not_null
#      table-prefix: t_
#  config-location: classpath:mybatis-config.xml
#  type-aliases-package: com.cesar.mpintroduction.entity

